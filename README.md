# Matrix-Dynamics-Explorer-Murray-Baginski

Pull repository onto machine
Install pipenv with the command 'pip install pipenv'
Run 'pipenv shell' to load virtual enviroment
Install requirements with 'pipenv install -r requirements.txt'
Run the command 'python manage.py runserver' from within pipenv to run server
Open localhost:8000 on web browser to use application
